---
title: Modulation
description: Animate any parameter with low-frequency oscillators.
---

LFOs (low-frequency oscillators) continuously modulate a parameter over time - wobbling a filter cutoff, pulsing the gain, or sweeping the pan. Attach one or an array to any layer. They're automatically cleaned up when the voice ends.

## Basic usage

```ts
{
  source: { type: "sawtooth", frequency: 80 },
  filter: { type: "lowpass", frequency: 600, resonance: 8 },
  gain: 0.3,
  lfo: {
    type: "sine",
    frequency: 6,
    depth: 500,
    target: "filter.frequency",
  },
}
```

<Demo
  label="Filter wobble"
  definition={{ source: { type: "sawtooth", frequency: 80 }, filter: { type: "lowpass", frequency: 600, resonance: 8 }, envelope: { attack: 0.01, decay: 0.6 }, gain: 0.3, lfo: { type: "sine", frequency: 6, depth: 500, target: "filter.frequency" } }}
  controls={[
    { param: "lfo.frequency", label: "Rate", min: 0.5, max: 20, step: 0.5 },
    { param: "lfo.depth", label: "Depth", min: 50, max: 2000, step: 10 },
    { param: "filter.resonance", label: "Resonance", min: 0, max: 20, step: 0.5 },
  ]}
/>

## Multiple LFOs

Stack modulations on a single layer:

```ts
{
  lfo: [
    { type: "sine", frequency: 4, depth: 300, target: "filter.frequency" },
    { type: "triangle", frequency: 0.5, depth: 0.1, target: "gain" },
  ],
}
```

## Parameters

| Property    | Type     | Description                  |
| ----------- | -------- | ---------------------------- |
| `type`      | `string` | Waveform: sine, triangle, square, sawtooth |
| `frequency` | `number` | LFO rate in Hz               |
| `depth`     | `number` | Modulation amount             |
| `target`    | `string` | Which parameter to modulate  |

## Available targets

<DemoGrid>
  <Demo
    label="Vibrato"
    definition={{ source: { type: "sine", frequency: 440 }, envelope: { attack: 0.05, decay: 0.8 }, gain: 0.3, lfo: { type: "sine", frequency: 5, depth: 20, target: "frequency" } }}
    controls={[
      { param: "lfo.frequency", label: "Rate", min: 0.5, max: 15, step: 0.5 },
      { param: "lfo.depth", label: "Depth", min: 5, max: 100, step: 1 },
    ]}
  />
  <Demo
    label="Tremolo"
    definition={{ source: { type: "triangle", frequency: 440 }, envelope: { attack: 0.05, decay: 0.8, sustain: 0.3 }, gain: 0.3, lfo: { type: "sine", frequency: 8, depth: 0.15, target: "gain" } }}
    controls={[
      { param: "lfo.frequency", label: "Rate", min: 0.5, max: 20, step: 0.5 },
      { param: "lfo.depth", label: "Depth", min: 0.01, max: 0.5, step: 0.01 },
    ]}
  />
  <Demo
    label="Auto-pan"
    definition={{ source: { type: "triangle", frequency: 440 }, envelope: { attack: 0.05, decay: 1 }, gain: 0.3, lfo: { type: "sine", frequency: 2, depth: 0.8, target: "pan" } }}
    controls={[
      { param: "lfo.frequency", label: "Rate", min: 0.1, max: 8, step: 0.1 },
      { param: "lfo.depth", label: "Width", min: 0.1, max: 1, step: 0.01 },
    ]}
  />
  <Demo
    label="Filter sweep"
    definition={{ source: { type: "sawtooth", frequency: 110 }, filter: { type: "lowpass", frequency: 800, resonance: 8 }, envelope: { attack: 0.01, decay: 0.8 }, gain: 0.25, lfo: { type: "sine", frequency: 4, depth: 600, target: "filter.frequency" } }}
    controls={[
      { param: "lfo.frequency", label: "Rate", min: 0.5, max: 12, step: 0.5 },
      { param: "lfo.depth", label: "Depth", min: 100, max: 2000, step: 10 },
    ]}
  />
</DemoGrid>

| Target              | What it does                    |
| ------------------- | ------------------------------- |
| `frequency`         | Pitch vibrato                   |
| `detune`            | Fine pitch modulation           |
| `gain`              | Tremolo / volume swell          |
| `pan`               | Auto-pan                        |
| `filter.frequency`  | Wah / filter sweep              |
| `filter.detune`     | Filter pitch shift              |
| `filter.Q`          | Resonance modulation            |
| `filter.gain`       | Shelf/peaking boost modulation  |
| `playbackRate`      | Speed modulation (samples)      |
